[pytest]
testpaths = tests
pythonpath = .
addopts = -ra
markers =
    db: needs Postgres; uses TEST_DATABASE_URL (default: an agent_test database on the compose db)
    ui: drives the chat page in a real browser (Playwright)
    live: calls real Connect AI; opt in with RUN_LIVE_TESTS=1
