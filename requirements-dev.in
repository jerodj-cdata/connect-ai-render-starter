# Test-only dependencies, constrained to the app's pins. Regenerate with:
#   uv pip compile requirements-dev.in --universal --python-version 3.12 -o requirements-dev.txt
-c requirements.txt
pytest
playwright
